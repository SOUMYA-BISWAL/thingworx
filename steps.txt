1.go to setting.py inside project folder
    add line ALLOWED_HOSTS = ['*']

2.run bellow command :
    python manage.py runserver localhost:8080

3. run below command
    python manage.py startapp captureshot

4. put below code inside "views.py" inside your project directory
    from django.shortcuts import render
    from django.http import HttpResponse

    # Create your views here.
    def index(request):
        return HttpResponse('Hell you are the polls index')

5. create "urls.py" inside your project directory which you have created recently and put below code
    from django.urls import path
    from captureshot import views

    urlpatterns = [
        path('', views.index),
    ]

6. go to urls.py and modify below code
    from django.contrib import admin
    from django.urls import path, include

    urlpatterns = [
        path('admin/', admin.site.urls),
        path('captureshot/', include('captureshot.urls')),
    ]
7. again run below command
    python manage.py runserver localhost:8080

8. put below command in view.py for index.html
    from django.shortcuts import render
    from django.http import HttpResponse

    # Create your views here.
    def index(request):
        #return HttpResponse('Hell you are the polls inderx')
        return render(request, "../templates/index.html")

9. modified setting.py as mention below code
    TEMPLATES = [
        {
            'BACKEND': 'django.template.backends.django.DjangoTemplates',
            'DIRS': [os.path.join(BASE_DIR, 'templates')]
            ,
            'APP_DIRS': True,
            'OPTIONS': {
                'context_processors': [
                    'django.template.context_processors.debug',
                    'django.template.context_processors.request',
                    'django.contrib.auth.context_processors.auth',
                    'django.contrib.messages.context_processors.messages',
                ],
            },
        },
    ]

10. create templates folder

11. create sample static html code inside template page
    <!DOCTYPE html>
    <html lang="en">
    <head>
        <meta charset="UTF-8">
        <title>Title</title>
    </head>
    <body>
    <h1>soumyabiswal {{ arg }}</h1>
    <h1>{{ arg }}</h1>
    </body>
    </html>

12. change view.py and give dynamic argument

    from django.shortcuts import render
    from django.http import HttpResponse

    # Create your views here.
    def index(request):
        #return HttpResponse('Hell you are the polls inderx')
        return render(request, "../templates/home.html", {'arg':'argument'})

13. add below code in "setting.py"
    INSTALLED_APPS = [

    'captureshot.apps.CaptureshotConfig',
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',
    ]

14. add database details inside "setting.py" file

    DATABASES = {
        'default': {
            'ENGINE': 'django.db.backends.postgresql',
            'NAME': 'photography',
            'USER':'postgres',
            'PASSWORD':'postgres',
            'HOST':'localhost',
            'PORT':'5432'
        }
    }
15. install postgres db and create DB "photography"

16. install dab and django connector "psycopg2" and "pillow" for image store
    --> file --> setting --> project --> project interpreter

17. put bellow code into model.py for creating table model
    from django.db import models

    # Create your models here.

    class Header(models.Model):
        name = models.CharField(max_length=100)
        img = models.ImageField(upload_to='pics')
        number = models.IntegerField(max_length=100)
        mail = models.CharField(max_length=100)
        password = models.CharField

18. then run bellow command
    python manage.py makemigrations
    then you will get message
    Migrations for 'captureshot':
    captureshot\migrations\0001_initial.py
    - Create model Header

19. you will get a file inside migration folder
    0001_initial.py

20. inside file you will get below code
    # Generated by Django 3.0.2 on 2020-01-29 17:50
    from django.db import migrations, models

    class Migration(migrations.Migration):

        initial = True

        dependencies = [
        ]

        operations = [
            migrations.CreateModel(
                name='Header',
                fields=[
                    ('id', models.AutoField(auto_created=True, primary_key=True, serialize=False, verbose_name='ID')),
                    ('name', models.CharField(max_length=100)),
                    ('img', models.ImageField(upload_to='pics')),
                    ('number', models.IntegerField(max_length=100))
                    ('mail', models.CharField(max_length=100)),
                    ('password', models.CharField(max_length=100)),
                ],
            ),
        ]


21. run below command for query table
    python manage.py sqlmigrate captureshot 0001

    (output:-
        BEGIN;
        --
        -- Create model Header
        --
        CREATE TABLE "captureshot_header" ("id" serial NOT NULL PRIMARY KEY, "name" varchar(100) NOT NULL, "img" varchar(100) NOT NULL, "number" integer NOT NULL, "mail" varchar(100) NOT NULL, "
        password" varchar(100) NOT NULL);
        COMMIT;
    )


22. then migrate query
    python manage.py migrate


